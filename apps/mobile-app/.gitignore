################################
# Dependencies
################################
node_modules/

################################
# Expo & Metro
################################
.expo/
.expo-export-dev/
.expo-export-temp/
.expo-export-temp2/
dist/
expo-env.d.ts

# Metro bundle cache
.metro_live_bundle.js
.metro-bundler/

################################
# Environment (never commit secrets)
################################
.env
.env.local
.env.*.local

################################
# Android build artifacts
################################
android/build/
android/app/build/
android/app/.cxx/
android/.gradle/
android/.kotlin/
android/local.properties

################################
# iOS build artifacts
################################
ios/build/
ios/Pods/
ios/*.xcworkspace
ios/DerivedData/

################################
# OS / Editor junk
################################
.DS_Store
Thumbs.db
.vscode/
.idea/

################################
# EAS / Expo Updates
################################
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli
# @end expo-cli

################################
# Firebase Service Account Keys (NEVER commit - these are actual secrets)
################################
serviceAccountKey.json
# NOTE: google-services.json and GoogleService-Info.plist are NOT secrets.
# They are client config files. Google explicitly says it's safe to commit them.
# They MUST be committed so EAS Build can access them.
# google-services.json   <-- intentionally NOT ignored
# GoogleService-Info.plist  <-- intentionally NOT ignored
